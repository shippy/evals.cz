---
---

<section class="format">
	<div class="container">
		<h2 class="section-title">Formát meetupu</h2>
		<div class="format-content">
			<div class="format-details">
				<div class="format-item">
					<span class="format-label">Místo</span>
					<span class="format-value">Praha (místo bude upřesněno)</span>
				</div>
				<div class="format-item">
					<span class="format-label">Formát</span>
					<span class="format-value">1-3 krátké přednášky + diskuse</span>
				</div>
				<div class="format-item">
					<span class="format-label">Jazyk</span>
					<span class="format-value">Především angličtina</span>
				</div>
				<div class="format-item">
					<span class="format-label">Atmosféra</span>
					<span class="format-value">Neformální, přátelská, praktická</span>
				</div>
			</div>
			<div class="format-philosophy">
				<blockquote>
					"Co se pokazilo v produkci a jak jsme to opravili"
					<span class="quote-attribution">— žádné sales pitche</span>
				</blockquote>
			</div>
		</div>
	</div>
</section>

<style>
	.format {
		background: var(--white);
		border-top: var(--border-standard) solid var(--black);
		position: relative;
		padding: 6rem 0;
	}

	.format::before {
		content: '04';
		position: absolute;
		bottom: 2rem;
		right: 4rem;
		font-size: 15rem;
		font-weight: 800;
		font-family: var(--font-display);
		color: var(--gray-90);
		z-index: 0;
	}

	.section-title {
		text-align: left;
		margin-left: 2rem;
		font-family: var(--font-display);
		text-transform: uppercase;
		font-size: clamp(2rem, 5vw, 3rem);
		font-weight: 700;
		margin-bottom: 3rem;
	}

	.format-content {
		display: flex;
		flex-direction: column;
		gap: 4rem;
		position: relative;
		z-index: 1;
	}

	.format-details {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 0;
	}

	.format-item {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		padding: 2rem;
		background: transparent;
		border: 2px solid var(--black);
		margin: -2px;
	}

	.format-item:nth-child(odd) {
		background: var(--gray-90);
	}

	.format-label {
		font-family: var(--font-ui);
		font-weight: 600;
		font-size: 0.85rem;
		color: var(--black);
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.format-value {
		font-family: var(--font-display);
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--black);
		text-transform: uppercase;
	}

	.format-philosophy {
		display: flex;
		align-items: center;
	}

	blockquote {
		font-family: var(--font-display);
		font-style: normal;
		font-size: 2rem;
		line-height: 1.2;
		text-transform: uppercase;
		color: var(--white);
		padding: 3rem;
		background: var(--black);
		border-left: var(--border-heavy) solid var(--accent);
	}

	.quote-attribution {
		display: block;
		font-family: var(--font-mono);
		font-size: 0.9rem;
		font-style: normal;
		text-transform: none;
		color: var(--accent);
		margin-top: 1rem;
	}

	@media (max-width: 768px) {
		.format::before {
			font-size: 6rem;
		}

		.section-title {
			margin-left: 0;
		}

		.format-details {
			grid-template-columns: 1fr;
		}
	}
</style>

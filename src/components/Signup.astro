---
---

<section id="signup" class="cta-section">
	<div class="container">
		<div class="cta-card">
			<h2>Chci být u dalšího meetupu</h2>
			<p>Přihlaste se k odběru zpráv o dalších Evals.cz meetupech</p>
			<a href="https://forms.gle/15Mrvkt8gxjp5xJk9" target="_blank" class="btn btn-primary">Přihlásit se</a>
		</div>
	</div>
</section>

<style>
	.cta-section {
		background: var(--black);
		color: var(--white);
		border-top: var(--border-heavy) solid var(--accent);
		border-bottom: var(--border-heavy) solid var(--accent);
		position: relative;
		padding: 6rem 0;
	}

	.cta-section::before {
		content: '05';
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		font-size: 20rem;
		font-weight: 800;
		font-family: var(--font-display);
		color: var(--white);
		opacity: 0.15;
		z-index: 0;
	}

	.cta-card {
		text-align: center;
		max-width: 600px;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.cta-card h2 {
		font-family: var(--font-display);
		font-size: clamp(1.75rem, 4vw, 2.5rem);
		text-transform: uppercase;
		color: var(--white);
		margin-bottom: 1rem;
	}

	.cta-card p {
		font-family: var(--font-mono);
		font-size: 1.1rem;
		margin-bottom: 2rem;
		color: var(--gray-90);
	}

	@media (max-width: 768px) {
		.cta-section::before {
			font-size: 6rem;
		}
	}
</style>
